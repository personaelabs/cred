import { createRpcClient } from '@cred/shared';

// const rpcClient = createRpcClient('https://cred-indexer-41hd.onrender.com');
const rpcClient = createRpcClient('http://0.0.0.0:3030');

export default rpcClient;
